#include <stdio.h>

/**
 * main - prints an out put using puts function
 * @*messages: a str/char tpe that is a parameter to puts function
 *
 * stdio: allows program to use standard input/output
 * Return: returns 0
 */
int main(void)
{
	const char *message = "\"Programming is like building \
a multilingual puzzle";

	puts(message);
	return (0);
}
