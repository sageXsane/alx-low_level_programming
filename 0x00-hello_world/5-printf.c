#include <stdio.h>

/**
 * main - prints an out put using printf function
 *
 * stdio: allows program to use standard input/output
 * Return: returns 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
