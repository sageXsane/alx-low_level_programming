#include "main.h"
#include <unistd.h>

/**
 * add - add two integers and return the sum
 * @a: first integer
 * @b: second integer
 *
 * Return: returns the sum of a and b
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
